websockets>=12.0
httpx>=0.27.0
numpy>=1.26.0
sounddevice>=0.4.6
# Pin to the v3 SDK line — v4+ (and especially v7) is a fully rewritten,
# incompatible API surface (no LiveTranscriptionEvents / LiveOptions).
deepgram-sdk>=3.7.0,<4
groq>=0.13.0,<1
pydantic>=2.7.0
pydantic-settings>=2.3.0
certifi>=2024.0.0

